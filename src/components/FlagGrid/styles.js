import { makeStyles} from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
    root: {
        flexGrow: 1,
        marginTop: 100,
        position: 'relative',
        marginLeft: 50, 
        marginRight: 50
    },
}));

export default useStyles